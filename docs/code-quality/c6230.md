---
title: C6230
ms.date: 11/04/2016
ms.topic: reference
f1_keywords:
- C6230
helpviewer_keywords:
- C6230
ms.assetid: aa91291d-cdc5-4720-89d4-194ce0557e99
author: mikeblome
ms.author: mblome
manager: markl
ms.workload:
- multiple
ms.openlocfilehash: 755e23d5854f187b7e86a6fe37c7668a8943f81d
ms.sourcegitcommit: 485ffaedb1ade71490f11cf05962add1718945cc
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 10/16/2019
ms.locfileid: "72445029"
---
# <a name="c6230"></a>C6230

Ostrzeżenie C6230: niejawne rzutowanie pomiędzy semantycznie różnymi typami całkowitymi: użycie HRESULT w kontekście Boolean

To ostrzeżenie wskazuje, że wartość "bCzy `HRESULT`" jest używana w kontekście, w którym oczekiwano wyniku logicznego, takiego jak instrukcja `if`. Ten test prawdopodobnie spowoduje zwrócenie niepoprawnych wyników. Na przykład typowa wartość sukcesu dla `HRESULT` (`S_OK`) ma wartość false, gdy jest ona testowana jako wartość logiczna.

## <a name="example"></a>Przykład

Poniższy kod generuje to ostrzeżenie:

```cpp
#include <windows.h>

VOID f( )
{
  LPMALLOC pMalloc;
  HRESULT hr = CoGetMalloc(1, &pMalloc);

  if (hr)
  {

    // code ...
  }
  else
  {
    // code ...
  }
}
```

W większości sytuacji makro [zakończone powodzeniem](/windows/desktop/api/winerror/nf-winerror-succeeded) lub [zakończone niepowodzeniem](/windows/desktop/api/winerror/nf-winerror-failed) powinno być używane do testowania wartości `HRESULT`. Aby poprawić to ostrzeżenie, należy użyć następującego kodu:

```cpp
#include <windows.h>

VOID f( )
{
  LPMALLOC pMalloc;
  HRESULT hr = CoGetMalloc(1, &pMalloc);

  if (SUCCEEDED(hr))
  {

    // code ...
  }
  else
  {
    // code ...
  }
}
```

Dla tego ostrzeżenia typ `SCODE` jest traktowany jako `HRESULT`.

Użycie `malloc` i `free` (i związanych z nimi interfejsów API pamięci dynamicznej) ma wiele pułapek jako przyczyn przecieków pamięci i wyjątków. Aby uniknąć tego rodzaju przecieków i problemów z wyjątkami, użyj wskaźników i klas kontenerów udostępnianych przez C++ bibliotekę Standard. Należą do nich [shared_ptr](/cpp/standard-library/shared-ptr-class), [unique_ptr](/cpp/standard-library/unique-ptr-class)i [Vector](/cpp/standard-library/vector). Aby uzyskać więcej informacji, zobacz [inteligentne wskaźniki](/cpp/cpp/smart-pointers-modern-cpp) i [ C++ standardowa biblioteka](/cpp/standard-library/cpp-standard-library-reference).
